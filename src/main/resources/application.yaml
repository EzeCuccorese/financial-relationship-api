# Database configuration
spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/reba
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
